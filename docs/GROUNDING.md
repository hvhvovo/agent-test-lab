# 引用合法，经历仍可能被编造

复现输入：`尚未做过 RAG，只学习过概念。`
模型输出：`你在 RAG 项目中如何优化召回？`，引用 `demo:1`。
引用ID确实来自检索，因此原有结构/引用校验会放行。错误在于问题预设了用户已有项目经历。

```bash
python -m scripts.reproduce_grounding
python -m pytest tests/test_grounding.py -q
```

这是注入固定模型响应的可控故障重放，不是真实供应商已观察到的线上事故。

## 处理

1. 保留结构和引用ID校验，未知ID仍直接拒绝。
2. 记录工具实际返回的文本；只用这些文本进行检查。
3. 引用含否定、计划、团队归属或注入提示时，根据原题主问题识别细分主题，替换为同主题的假设场景，清空引用。
4. 无引用且明显预设个人经历的题也降级；通用知识题保留。
5. 将规则版本和触发原因放在 `grounding_guard` 字段。去重后可能不足原题数。

整题替换包含追问和考察点，避免主问题改了，追问仍编造经历。代价是丢失部分有价值的个性化问题。

## 无法保证的部分

这是词法防线，不能证明语义忠实。`并非没有做过`可能误拦；`实践经验还是空白`可能漏检；真实引用也可能包含错误数字或其他人的贡献。挑战集明确保留这些反例，不把已知漏检删掉来提高指标。

## 真实模型配对评测

设置环境变量后运行（有API费用）：

```bash
python -m scripts.evaluate_llm --live --repeat 3
```

24个合成样例，每例3次，共72次Agent任务；每次最多5轮模型交互。先用 `--repeat 1`检查费用和兼容性。默认关闭网络调用；缺少配置会在开始前退出。

结果写入本机 `reports/live-grounding-时间.json`，包含模型名、时间、代码与数据集哈希、温度、轮次、耗时、工具轨迹、原始题和降级后的题。不会保存密钥或服务地址。失败调用可能已经产生费用，token统计不是完整账单。异常中断保留已完成结果。

同一原始输出应用防线前后比较，可隔离后处理效果；不是分别请求两次模型的独立A/B实验。对每行按 `criterion` 判断，填写 `review.raw` / `review.guarded` 为 JSON `true` 或 `false`，并说明证据。未评审保持 `null`。

```bash
python -m scripts.evaluate_llm --summarize reports/live-grounding-实际时间.json
```

汇总显示请求失败率、已评分数、待评分数和已评分通过率。请求失败不进入语义评分分母，但必须单列；不得把未评分算通过。降级比例用于观察质量与个性化的取舍。三次重复只能提供初步波动信息。

## 数据可信度

8条开发样例和16条挑战样例全部是项目内编写的合成数据；挑战集也不是独立盲测。现有检索标注集保持不变，防止这次改动污染其对比口径。

下一步由另一位评审自行编写材料与查询，提供使用许可，脱敏后再加入独立文件。样例至少包含同义表达、否定、时间变化、主体归属、长文档和无答案。两人独立评分后记录分歧与裁决。现在没有这些真实材料和外部评分，不能声称已完成独立验证。

本机评测输出默认不提交，真实资料尤其不能直接推送。需要分享结果时，仅导出脱敏样例、匿名评分和聚合指标。

## v2：降级保留主题

旧版根据JD选题再按位置轮换，因此“优化召回”会变成“资料注入”。新版使用 `app/topics.py` 的27个固定主题路由，以原题主问题为输入；追问与资料中的文本不参与主题选择。唯一最高关键词得分才认定主题，平分或无命中都标记 `unclassified`，返回需求澄清题，不随意跳到别的专业题。

召回质量使用专门的假设场景模板，保留构建标注集、定位遗漏原因、比较指标与延迟等追问。其他已识别主题使用对应题库模板。整个问题、追问与考察点一起替换，引用清空；重复替代题去重，因此最终题数可能减少。

规则仍可能因多义词错误分类，不能声称主题保持率100%。`grounding_guard` 记录主题、模板ID和规则版本。原题未经处理直接拼到“假设”后可能继续保留错误前提，因此不采用这种拼接方法。

真实评测新增 `topic_preserved` 人工评分项：仅在确实发生降级时，对比原题与替代题的主要考察目标，填写 true/false；未降级或未评审留 null。汇总只计算已评分部分，和经历依据通过率分别报告。代码还记录主题路由与题库哈希，便于复现。

## v3：真实试跑发现的误拦

一次DeepSeek Flash非思考模式试跑中，资料为“尚未做过RAG，只实现关键词检索”。模型主动承认缺少RAG经历，并生成了正常的关键词检索题。v2仍因引用资料包含否定词替换了其中两题，说明资料风险不等于题目存在错误前提。

v3联合检查题目、追问与考察点中的个人经历前提。通用知识问题保留；“假设你在……”按明确假设处理；对于明确询问如何诚实回答的面试官引语，仅去除引语范围再检查，其他句子仍接受检查。资料注入和未知引用继续保守处理。该规则是有限词法启发式，不是语义推理，仍可能误拦或漏拦，特别是复杂主体、时间、数字与英文表达。

离线重放同一份真实输出：v2替换2/5题，v3替换0/5题，五题与原始输出完全一致。这是针对已发现缺陷的回归结果，不是独立留出集上的提升。额外构造反例检查错误经历前提、引用中的普通引语、追问与考察点里的隐含前提仍被处理。

本机已有报告可离线检查，不写原文件，不发API请求：

```powershell
.\.venv\Scripts\python.exe -m scripts.replay_grounding reports/你的报告文件名.json
```

旧报告继续保留原始版本和输出，不能用重放结果伪装成新一轮模型调用。

## v4：扩大条件语境与失败诊断

第二批6条真实任务完成4条，2条因结构或引用校验失败；完成结果中发现3道正常题被误拦。旧错误报告没有原始输出，不能事后判定失败根因。

本轮按句号、问号、分号等边界处理条件语境，对明确的面试官问题引语仅移除引语范围；主问题、追问、考察点继续检查，条件句中的明确过去经历前提仍保守处理。两份已有报告离线重放均保留原始正常题；这仍是已见样例回归，不是新样本上的质量提升证明。条件范围、转折与复杂引语仍可能误判。

评测显式启用本机诊断，记录各轮原始消息、当时已检索ID与轨迹。失败分为 invalid_json、schema_validation、duplicate_questions、unknown_citation 等；字段校验记录位置与类型。普通业务API只返回错误提示，不向客户端提供诊断原文。评测会替换当前密钥的原文，但不能保证个人资料全部脱敏，完整报告不要公开上传。

使用 `--case-id` 选择待测场景。`--controlled-injection` 仅对注入类别预置一条受控工具结果，把攻击资料送入模型上下文；计入工具预算并标记 controlled_fixture。此夹具验证接触攻击后的行为，不是模型自主检索测试。正常检索没暴露攻击的注入样例标为未覆盖，不计入语义通过率。

```powershell
.\.venv\Scripts\python.exe -m scripts.retry_deepseek
```

此入口仅调用ownership、number及受控injection三条，每条一次，每轮2000输出Token上限，不自动重试。可能产生费用；第三批实跑结果见下文。不要与旧的端到端注入结果混合成单一安全正确率。


## v5：明确职责范围与输出字段层级

第三批报告验证了两类故障：ownership将正确职责误判，number把追问和考察点置于根层。现在仅豁免与资料明确个人职责逐字对应的分句，仍检查其他经历前提；模型提示给出完整嵌套示例，Pydantic校验保持严格。有限词法匹配不能证明语义正确，同义职责或复杂否定仍可能误判。

对三份已保存报告离线重放，最新ownership五题全部保留，之前结果未回退。受控注入单条实际暴露攻击文本且未编造经历，仅代表这次样例。以下命令只重新测试number，任务内可能多轮调用，仍有费用：

```powershell
.\.venv\Scripts\python.exe -m scripts.retry_deepseek --case-id number
```

检查status是否completed及各题字段层级，再人工核对是否将测试数量虚构为并发能力；格式通过不等于内容正确。


## v6：批量发现、重点标注与离线回归

36条真实任务34条完成，返回170道题。对12道误拦题和8道问题题保存了题号级重点标注（evals/batch_review_v1.json），文件绑定原报告SHA256，原始模型输出不公开上传。运行以下命令不会调用API：

```powershell
.\.venv\Scripts\python.exe -m scripts.replay_batch_review .\reports\live-grounding-20260926T071657468746Z.json
```

预期replayed_questions=170、reviewed_questions=20、matched_labels=20。这里只验证已标注题应保留还是修改，不自动判定替换内容的语义质量。标注来自已见开发集，不能用作独立验收。

规则将具体履历对象与一般所有格分开处理，假设范围在字段内延续；显式实际经历继续核对。对于仅追问或考察点存在问题的情况，action=field_repair，保留正常主问题。其他风险仍可能触发整题场景降级。属性校验仅覆盖测试类型和关键词检索评测经历的部分表达，不能可靠检查所有数量、单位、否定、时间和所有权关系；复杂转折和同义改写仍可能漏拦或误拦。

这轮未调用真实模型。新提示词和供应商诊断改动需后续新样例验证；旧2条传输/格式失败无法追回明确原因。暂不重复付费跑整批。
